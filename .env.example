# Reference value only. The application reads VSA_DATA_ROOT from the process environment.
VSA_DATA_ROOT=D:/Database-PC

# Optional. One of DEBUG, INFO, WARNING, ERROR. Defaults to INFO.
VSA_LOG_LEVEL=INFO
